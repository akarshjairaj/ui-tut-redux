// Nodes Individual state
const initialCounterState = { value: 0 };

// Root initial state
const initialState = { counter: { ...initialCounterState } };

export { initialCounterState };
export default initialState;
